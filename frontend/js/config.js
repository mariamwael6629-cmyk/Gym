// Backend API base URL. Override by setting window.API_BASE_URL before this
// script loads (e.g. in a deployment-specific <script> tag) if the backend
// is hosted somewhere other than localhost:8000.
window.API_BASE_URL = window.API_BASE_URL || 'http://localhost:8000';
